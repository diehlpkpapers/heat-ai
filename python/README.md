# Numericsl integration

```bash
This code uses the quad function from the scipy.integrate module to perform numerical integration. Make sure you have NumPy and SciPy installed (pip install numpy scipy).
```
